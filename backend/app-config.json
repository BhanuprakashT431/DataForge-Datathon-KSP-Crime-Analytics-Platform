{
  "name": "KSP_Crime_Intel_Backend",
  "env": "python39",
  "command": "uvicorn main:app --host 0.0.0.0 --port $X_ZOHO_CATALYST_LISTEN_PORT",
  "build": "pip install -r requirements.txt",
  "memory": 512,
  "timeout": 30
}
